'use client';

import { motion, AnimatePresence } from 'framer-motion';

export const anim = AnimatePresence;

export const div = motion.div;
export const section = motion.section;
export const nav = motion.nav;
export const path = motion.path;